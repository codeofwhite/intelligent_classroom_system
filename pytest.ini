[pytest]
# 测试文件发现规则
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 默认标记
markers =
    slow: 标记为慢速测试（如视频上传分析）
    smoke: 冒烟测试（快速验证核心功能）
    stress: 压力测试相关

# 输出设置
addopts = -v --tb=short --strict-markers